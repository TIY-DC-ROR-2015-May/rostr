<h1><%= @team.name %></h1>

<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th>Name</th>
    </tr>
  </thead>

  <tbody>
    <% @team.players.each do |player| %>
      <tr>
        <td><%= player.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>